# Profile Images

Drop your profile/avatar photo here and name it `avatar.jpg` (or `.png`).

The site will automatically use it in the ProfileCard.
If the file is missing it falls back to a gradient "CL" monogram.

Recommended: square crop, at least 200×200px.
